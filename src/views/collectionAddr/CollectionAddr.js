import React from 'react'
import { View, Text } from 'react-native'

const CollectionAddr = props => {
    return (
        <View>
            <Text>CollectionAddr</Text>
        </View>
    )
}

export default CollectionAddr