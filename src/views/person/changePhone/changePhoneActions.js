import reduxActionTypes from '../../../reduxActionTypes'
import httpRequest from '../../../utils/HttpRequest'
import * as host from '../../../utils/host'

export const changePhone = () => async (dispatch, getState) => {
    try{

    }catch(err){

    }
}

export const getVerifChangePhoneCode = () => async (dispatch, getState) => {
    try{

    }catch(err){
        
    }
}